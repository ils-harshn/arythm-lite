import DocumentMeta from "react-document-meta";

const MusicSelection = () => {
  const meta = {
    title: "Arythm Lite - Music Selection",
  };

  return <DocumentMeta {...meta}></DocumentMeta>;
};

export default MusicSelection;
